package model;

/**
 * Bonus: Create a singleton bean A which has a getter for bean B (getB). and bean B.
 * i.	 Each time you call getB(), it should get a new instance of B.
 * ii.	Implement this in 2 ways.
 * iii.	Hint: option 1: use ‘lookup-method’ element in bean A. option 2:  configure ObjectFactory on bean A that will create bean B.
 */
public class TaskG {
}
